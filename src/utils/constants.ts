import { StylesConfig } from "react-select";

export const stylesSelect: StylesConfig = {
    control: (styles) => ({ ...styles, borderRadius: '0.5rem', minHeight: '3rem', fontSize: '0.875rem', alignContent: 'center' }),
}